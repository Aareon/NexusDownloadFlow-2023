repos:
  - repo: local
    hooks:
      - id: uv-lock-check
        name: Ensure uv.lock is up to date
        entry: uv lock --check
        language: system
        pass_filenames: false
        always_run: true
